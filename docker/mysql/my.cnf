[mysqld]
# MySQL 8.4 で mysql_native_password プラグインを有効化
# (デフォルトでは非推奨・無効化されている)
mysql_native_password=ON

# 文字コード設定
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci

# タイムゾーン
default-time-zone='+09:00'

[client]
default-character-set=utf8mb4
